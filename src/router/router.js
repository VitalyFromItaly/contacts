import Vue from 'vue'
import Router from 'vue-router'

Vue.use(Router);
let router = new Router({
    mode: 'history',
    routes: [{}],
    //scrollBehavior() {
    //    return { y: 0 }
    //}
})

export default router;